<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Work Hours Tracker</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<style>
  .navbar {
  background-color: #2c4896; /* navy blue */
  color: white;
  box-shadow: 0 4px 6px rgba(0,0,0,0.1);
}
.navbar a {
  color: white;
}
.navbar a:hover {
  color: #f0f0f0;
}
</style>
<body>
  <!-- Navbar -->
  <nav class="navbar toFixed navbar-expand-lg ">
    <div class="container-fluid">
      <a class="navbar-brand d-flex align-items-center" href="/">
        <img src="https://img.icons8.com/?size=30&id=108792&format=png" alt="Logo" width="30" height="30" class="me-2">
        <span>Work Hours Tracker</span>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav mx-auto">
          <li class="nav-item"><a class="nav-link" href="/">🏠 Home</a></li>
          <li class="nav-item"><a class="nav-link" href="/about_us">ℹ️ About</a></li>
          <% if (username) {%>
          <li class="nav-item">
              <a class="nav-link" href="/JobEntry">Add Job-data</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/JobData">show Job-data</a>
            </li>
          <% } %> 
        </ul>

        <ul class="navbar-nav ms-auto">
          <% if (username) { %>
            <li class="nav-item">
              <span class="nav-link">👋 Welcome, <%= username %></span>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/logout">🚪 Logout</a>
            </li>
         
          <% } else { %>
            <li class="nav-item"><a class="nav-link" href="/login">🔑 Login</a></li>
            <li class="nav-item"><a class="nav-link" href="/register">📝 Register</a></li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Main Content -->
  <div class="container mt-5">
    
    <% if (username && (typeof showWelcome === 'undefined' || showWelcome)) { %>
      <h1>Welcome back, <%= username %>!</h1>
      <p>Use the navigation bar to explore your Work Hours Tracker dashboard.</p>
    <% } if (!username && (typeof showWelcome === 'undefined' || showWelcome)) { %>
      <h1>Welcome to Work Hours Tracker!</h1>
      <p>Please login or register to start tracking your work hours.</p>
    <% } %>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

       
</body>
</html>


